version: '3'

networks:
  backend:
    driver: bridge

services:

  node:
    build: .
    ports:
      - '3000:3000'
    volumes:
      - '.:/app'
    command: 'npm run dev'
    networks:
      - backend
